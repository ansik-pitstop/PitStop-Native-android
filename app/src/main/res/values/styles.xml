<resources>
    <!-- Base application theme. -->
    <style name="AppNoActionBarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="android:actionBarStyle">@style/ActionBarStyle</item>

    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/ActionBarStyle</item>
        <item name="colorAccent">@color/primary_light</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>

    </style>

    <style name="TestAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark_dark</item>

        <item name="android:textColorHint">@color/white_text</item>
    </style>

    <style name="TextInputTheme">
        <item name="colorAccent">@color/white_text</item>
        <item name="colorControlNormal">@color/white_text</item>
        <item name="colorControlActivated">@color/white_text</item>
        <item name="colorControlHighlight">@color/white_text</item>
    </style>


    <style name="ActionBarStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:background">@color/primary</item>
    </style>

    <style name="AddCarButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@drawable/color_button_primary</item>
    </style>

    <style name="TestAppButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@drawable/color_button_primary_dark</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="DialogAnimations"/>

    <style name="DialogAnimations.slide">
        <item name="@android:windowEnterAnimation">@anim/dialog_slide_in_right</item>
        <item name="@android:windowExitAnimation">@anim/dialog_slide_out_left</item>
    </style>

    <style name="DialogAnimations.grow">
        <item name="@android:windowEnterAnimation">@anim/dialog_grow_in</item>
        <item name="@android:windowExitAnimation">@anim/dialog_grow_out</item>
    </style>

</resources>
