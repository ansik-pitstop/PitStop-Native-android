package com.pitstop.models.trip;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LOCATION_START".
*/
public class LocationStartDao extends AbstractDao<LocationStart, Long> {

    public static final String TABLENAME = "LOCATION_START";

    /**
     * Properties of entity LocationStart.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "id", true, "_id");
        public final static Property Altitude = new Property(1, String.class, "altitude", false, "ALTITUDE");
        public final static Property Latitude = new Property(2, String.class, "latitude", false, "LATITUDE");
        public final static Property Longitude = new Property(3, String.class, "longitude", false, "LONGITUDE");
        public final static Property StartLocation = new Property(4, String.class, "startLocation", false, "START_LOCATION");
        public final static Property StartCityLocation = new Property(5, String.class, "startCityLocation", false, "START_CITY_LOCATION");
        public final static Property StartStreetLocation = new Property(6, String.class, "startStreetLocation", false, "START_STREET_LOCATION");
        public final static Property Trip = new Property(7, Long.class, "trip", false, "TRIP");
    }

    private DaoSession daoSession;


    public LocationStartDao(DaoConfig config) {
        super(config);
    }
    
    public LocationStartDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LOCATION_START\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL ," + // 0: id
                "\"ALTITUDE\" TEXT," + // 1: altitude
                "\"LATITUDE\" TEXT," + // 2: latitude
                "\"LONGITUDE\" TEXT," + // 3: longitude
                "\"START_LOCATION\" TEXT," + // 4: startLocation
                "\"START_CITY_LOCATION\" TEXT," + // 5: startCityLocation
                "\"START_STREET_LOCATION\" TEXT," + // 6: startStreetLocation
                "\"TRIP\" INTEGER);"); // 7: trip
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LOCATION_START\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LocationStart entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        String altitude = entity.getAltitude();
        if (altitude != null) {
            stmt.bindString(2, altitude);
        }
 
        String latitude = entity.getLatitude();
        if (latitude != null) {
            stmt.bindString(3, latitude);
        }
 
        String longitude = entity.getLongitude();
        if (longitude != null) {
            stmt.bindString(4, longitude);
        }
 
        String startLocation = entity.getStartLocation();
        if (startLocation != null) {
            stmt.bindString(5, startLocation);
        }
 
        String startCityLocation = entity.getStartCityLocation();
        if (startCityLocation != null) {
            stmt.bindString(6, startCityLocation);
        }
 
        String startStreetLocation = entity.getStartStreetLocation();
        if (startStreetLocation != null) {
            stmt.bindString(7, startStreetLocation);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LocationStart entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        String altitude = entity.getAltitude();
        if (altitude != null) {
            stmt.bindString(2, altitude);
        }
 
        String latitude = entity.getLatitude();
        if (latitude != null) {
            stmt.bindString(3, latitude);
        }
 
        String longitude = entity.getLongitude();
        if (longitude != null) {
            stmt.bindString(4, longitude);
        }
 
        String startLocation = entity.getStartLocation();
        if (startLocation != null) {
            stmt.bindString(5, startLocation);
        }
 
        String startCityLocation = entity.getStartCityLocation();
        if (startCityLocation != null) {
            stmt.bindString(6, startCityLocation);
        }
 
        String startStreetLocation = entity.getStartStreetLocation();
        if (startStreetLocation != null) {
            stmt.bindString(7, startStreetLocation);
        }
    }

    @Override
    protected final void attachEntity(LocationStart entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public LocationStart readEntity(Cursor cursor, int offset) {
        LocationStart entity = new LocationStart( //
            cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // altitude
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // latitude
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // longitude
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // startLocation
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // startCityLocation
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6) // startStreetLocation
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LocationStart entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setAltitude(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setLatitude(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setLongitude(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setStartLocation(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setStartCityLocation(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setStartStreetLocation(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(LocationStart entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(LocationStart entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(LocationStart entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getTripDao().getAllColumns());
            builder.append(" FROM LOCATION_START T");
            builder.append(" LEFT JOIN TRIP T0 ON T.\"TRIP\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected LocationStart loadCurrentDeep(Cursor cursor, boolean lock) {
        LocationStart entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Trip trip = loadCurrentOther(daoSession.getTripDao(), cursor, offset);
        entity.setTrip(trip);

        return entity;    
    }

    public LocationStart loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<LocationStart> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<LocationStart> list = new ArrayList<LocationStart>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<LocationStart> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<LocationStart> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
